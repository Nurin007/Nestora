server:
  port: 8085

spring:
  application:
    name: admin-service
  r2dbc:
    url: r2dbc:h2:file:./db/admin_db;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password: password
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
